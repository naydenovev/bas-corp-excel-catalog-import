# Полная выгрузка конфигурации BAS (не часть этой обработки, не публикуется)
bascorpxml/

# Локальные настройки подключения к базе разработчика
ПутьКБазе.txt

# Тестовые данные с реальными товарами заказчика — не публикуются
Тест загрузки.xlsx

# Локальная конфигурация инструментов разработки
.claude/

# Временные/диагностические файлы
_dump_*/
_err*/
*.tmp
