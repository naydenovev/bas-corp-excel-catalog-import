## 1. Scaffold внешней обработки

- [x] 1.1 Создать scaffold EPF `ЗагрузкаСправочникаИзExcel` (XML-исходники) в
      `src_epf/ЗагрузкаСправочникаИзExcel/` через `1c-epf-init`
- [x] 1.2 Добавить функцию `СведенияОВнешнейОбработке()` в модуль объекта
      (регистрация в БСП «Дополнительные отчёты и обработки», вид —
      произвольная команда открытия формы) через `1c-epf-bsp-init`

## 2. Управляемая форма — выбор справочника и реквизитов

- [x] 2.1 Создать управляемую форму с реквизитом `ВидСправочника` (тип —
      перечисление имён справочников или `ПланВидовХарактеристик`-подобный
      выбор через `Метаданные.Справочники`)
- [x] 2.2 Реализовать серверную процедуру построения списка реквизитов
      выбранного справочника (простые, ссылочные, `Родитель` при
      `Иерархический = Истина`, реквизиты табличных частей с префиксом) и
      вывод в таблицу формы «Карта сопоставления»
- [x] 2.3 Добавить в таблицу сопоставления колонку «Колонка файла» (ввод
      пользователем буквы/номера/заголовка колонки) и колонку-флажок «Ключ
      поиска»
- [x] 2.4 Реализовать проверку перед запуском: должен быть отмечен хотя бы
      один ключ поиска, иначе — сообщение и отказ от запуска

## 3. Чтение исходного файла

- [x] 3.1 Добавить на форму выбор файла (`ДиалогВыбораФайла`) и переключатель
      способа чтения («Без Excel» / «Через MS Excel»)
- [x] 3.2 Реализовать серверную функцию чтения через
      `ТабличныйДокумент.Прочитать()`, возвращающую массив строк-структур
      «имя колонки заголовка → значение»
- [x] 3.3 Реализовать клиентскую функцию чтения через
      `COMОбъект("Excel.Application")` с обязательным `Quit()` +
      `ОсвободитьОбъект()` в `Попытка...Исключение...Иначе`, проверкой
      толстого клиента перед запуском
- [x] 3.4 Свести оба способа к единому формату данных, проверить на
      тестовом xlsx-файле с заголовками

## 4. Логика поиска и сопоставления

- [x] 4.1 Реализовать построение запроса/поиска элемента справочника по
      значениям отмеченных ключевых реквизитов (точное совпадение,
      `СтрСравнить`/`ВРег` для строк)
- [x] 4.2 Реализовать разрешение ссылочных реквизитов и `Родитель` по коду
      связанного справочника (`НайтиПоКоду`, приведение к строке + `СокрЛП`)
      с явной ошибкой строки при ненайденном коде
- [x] 4.3 Реализовать разбор значений реквизитов табличной части из ячейки
      по разделителю `;` и построение строк ТЧ в заданном порядке

## 5. Запись: создание, диалог расхождений, протокол

- [x] 5.1 Реализовать ветку создания нового элемента (когда поиск по ключу
      не находит совпадения) с заполнением всех сопоставленных реквизитов
- [x] 5.2 Реализовать сравнение значений всех сопоставленных реквизитов
      найденного элемента со значениями файла, построение списка расхождений
- [x] 5.3 Реализовать диалоговую форму подтверждения расхождений (список с
      флажками «Применить» по каждому расходящемуся реквизиту)
- [x] 5.4 Реализовать применение подтверждённых изменений и запись элемента
- [x] 5.5 Реализовать формирование протокола (табличный документ на форме)
      со строками: Действие (Создан/Обновлён/Без изменений/Пропущен/Ошибка),
      номер строки файла, ключ, детали

## 6. Валидация и проверка работоспособности

- [x] 6.1 Собрать EPF из исходников (`1c-epf-build`), валидировать
      (`1c-epf-validate`)
- [x] 6.2 Подготовить тестовые Excel-файлы (иерархический справочник
      Номенклатура с `Родитель` по коду, ссылочными и Перечисление-реквизитами)
      и прогнать загрузку вручную в реальных базах BAS Бухгалтерія КОРП и
      BAS КУП (толстый/тонкий клиент)
- [x] 6.3 Проверить сценарий повторного запуска того же файла (дубли не
      создаются, расхождения корректно предлагаются к подтверждению)
- [x] 6.4 Проверить оба способа чтения (без COM и через COM Excel) на одном
      и том же файле — результат загрузки совпадает (после фиксов: чтение по
      букве/заголовку колонки, `.Text` вместо `.Value` для COM Excel)

## 7. Документация и публикация

- [x] 7.1 Написать README.md с описанием обработки, инструкцией подключения
      через БСП, ограничениями (разделитель `;`, отсутствие автосоздания
      групп) и отметкой «выполнено при помощи Claude Code»
- [x] 7.2 Создать GitHub-репозиторий `naydenovev/bas-corp-excel-catalog-import`,
      запушить исходники EPF + README
